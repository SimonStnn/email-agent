trigger:
  tags:
    include:
      - cerm-mcp-v*

pool: IaC

variables:
  - group: 'resource-access'
  - name: acrName
    value: 'pocmcpservercerm'
  - name: rgName
    value: 'POC-MCP'
  - name: service
    value: 'cerm-mcp'
  - name: context
    value: 'mcp-server'
  - name: containerInstanceName
    value: 'cerm-mcp-aci'
  - name: containerPort
    value: '8000'

steps:
  - checkout: self

  - bash: |
      # Login to Azure
      az login --service-principal -u $(clientId) -p $(clientSecret) --tenant $(tenantId) || exit 1

      # Set subscription
      az account set --subscription $(subscription)

      # Create resource group if not exists
      az group create --name $(rgName) --location swedencentral --only-show-errors || echo "Resource group already exists"

      # Register Microsoft.ContainerRegistry provider
      echo "Registering Microsoft.ContainerRegistry provider..."
      az provider register --namespace Microsoft.ContainerRegistry --wait

      # Create ACR if not exists
      az acr create --name $(acrName) --resource-group $(rgName) --sku Basic --only-show-errors || echo "ACR already exists"
    displayName: 'Create Resource Group and ACR if not exists'

  - script: |
      # Parse tag to get version
      TAG_NAME=$(Build.SourceBranchName)
      echo "Tag Name: $TAG_NAME"
      if [[ $TAG_NAME =~ ^cerm-mcp-v(.+)$ ]]; then
        VERSION=${BASH_REMATCH[1]}
        echo "Version: $VERSION"
      else
        echo "Invalid tag format"
        exit 1
      fi

      # Build image
      echo "Building image: $(service):$VERSION"
      docker build -t $(service):$VERSION $(context) || {
        echo "ERROR: Docker build failed"
        exit 1
      }

      # Verify image was built
      docker images $(service):$VERSION

      # Login to ACR
      echo "Logging into ACR: $(acrName)"
      az acr login --name $(acrName) || {
        echo "ERROR: ACR login failed"
        exit 1
      }

      # Tag and push to poc-mcp repository
      ACR_LOGIN_SERVER=$(acrName).azurecr.io
      IMAGE_NAME=$ACR_LOGIN_SERVER/poc-mcp/$(service)

      echo "Tagging and pushing to: $IMAGE_NAME"

      docker tag $(service):$VERSION $IMAGE_NAME:latest
      docker tag $(service):$VERSION $IMAGE_NAME:$VERSION
      docker tag $(service):$VERSION $IMAGE_NAME:${VERSION%.*}  # x.x
      docker tag $(service):$VERSION $IMAGE_NAME:${VERSION%%.*}  # x

      echo "Pushing images..."
      docker push $IMAGE_NAME:latest || { echo "ERROR: Push failed for latest"; exit 1; }
      docker push $IMAGE_NAME:$VERSION || { echo "ERROR: Push failed for $VERSION"; exit 1; }
      docker push $IMAGE_NAME:${VERSION%.*} || { echo "ERROR: Push failed for ${VERSION%.*}"; exit 1; }
      docker push $IMAGE_NAME:${VERSION%%.*} || { echo "ERROR: Push failed for ${VERSION%%.*}"; exit 1; }

      echo "Successfully pushed all tags to ACR"
    displayName: 'Build and Push Image'

  - bash: |
      set -e

      # Identity Variables
      UAI_NAME="cerm-mcp-puller-id"

      # 1. Create a User-Assigned Managed Identity
      echo "Creating User-Assigned Identity: $UAI_NAME..."
      az identity create --name $UAI_NAME --resource-group $(rgName)

      # 2. Get Resource IDs needed for permissions
      UAI_ID=$(az identity show --name $UAI_NAME --resource-group $(rgName) --query id -o tsv)
      UAI_PRINCIPAL_ID=$(az identity show --name $UAI_NAME --resource-group $(rgName) --query principalId -o tsv)
      ACR_ID=$(az acr show --name $(acrName) --resource-group $(rgName) --query id -o tsv)

      # 3. Grant AcrPull permission to this Identity
      echo "Granting AcrPull role..."
      # Retry loop to handle replication delays
      for i in {1..5}; do
        az role assignment create \
          --assignee $UAI_PRINCIPAL_ID \
          --scope $ACR_ID \
          --role AcrPull && break || sleep 5
      done

      # Wait briefly for permission propagation
      sleep 30

      # 4. Deploy Container
      ACR_LOGIN_SERVER=$(acrName).azurecr.io
      IMAGE_NAME=$ACR_LOGIN_SERVER/poc-mcp/$(service):latest

      echo "Deploying container with User-Assigned Identity..."

      # Clean up previous instance to ensure clean state
      if az container show --resource-group $(rgName) --name $(containerInstanceName) &>/dev/null; then
         echo "Deleting existing container..."
         az container delete --resource-group $(rgName) --name $(containerInstanceName) --yes
      fi

      # Create ACI
      # --acr-identity matches the error: it tells ACI "Use this specific ID to pull"
      az container create \
        --resource-group $(rgName) \
        --name $(containerInstanceName) \
        --image $IMAGE_NAME \
        --assign-identity $UAI_ID \
        --acr-identity $UAI_ID \
        --os-type Linux \
        --ip-address Public \
        --ports $(containerPort) \
        --dns-name-label $(containerInstanceName) \
        --cpu 1 \
        --memory 2 \
        --restart-policy Always \
        --location swedencentral

      # 5. Output Results
      FQDN=$(az container show \
        --resource-group $(rgName) \
        --name $(containerInstanceName) \
        --query ipAddress.fqdn -o tsv)

      echo "##############################################"
      echo "Deployment successful!"
      echo "Service URL: http://$FQDN:$(containerPort)"
      echo "##############################################"
    displayName: 'Deploy to Azure Container Instances (User-Assigned Identity)'
